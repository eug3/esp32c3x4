# ESP32-C3 多硬件按键处理方案

## 1. 概述

本固件是一个**通用固件**，通过检测设备型号标识符来自动适配两款不同的硬件设备。两款设备虽然使用相同的 MCU (ESP32-C3) 和按键检测方式（ADC），但按键布局和 ADC 阈值范围不同。

### 核心机制

- **按键检测方式**：ADC (模拟到数字转换器) 电压检测
- **返回键引脚**：GPIO1 (ADC1_CH1) - 两款设备相同
- **型号识别**：通过 `0x3FCAA029` 地址的值区分设备类型
- **配置存储**：使用 NVS/eepromUser 存储各按键的 ADC 校准值

---

## 2. 设备型号定义

| 设备标识 | 值 | 说明 |
|---------|-----|------|
| **设备 A** | `!= 3` | 默认设备类型 (包括 0xFF 未配置状态) |
| **设备 B** | `== 3` | 第二种设备类型 |

型号标识符存储在地址：`0x3FCAA029`
- 初始值：`0xFF`
- 运行时自动检测或配置

---

## 3. ADC 按键检测原理

### 3.1 硬件连接

按键通过电阻分压网络连接到 **ADC1 通道 1 (GPIO1)**：

```
VCC ──┬─[R1]─┬─ GPIO1/ADC1_CH1
      │       │
      │      ─┴─┬─ GND
      │         │
      │       [按键电阻网络]
      │
    ┌─┴─┐
    │   │
  [按键] [按键] [按键] [按键]
    │       │       │       │
   [R2]    [R3]    [R4]    [R5]
    │       │       │       │
    └───────┴───────┴───────┴── GND
```

每个按键按下时，产生不同的分压值，ADC 读取后转换为数字值。

### 3.2 ADC 读取函数

**函数地址**：`0x4200543C` (`sub_4200543C`)

```c
int __fastcall sub_4200543C(_WORD *a1)
{
    // 读取 ADC 通道 1 的原始值
    int adc_value = sub_420832FA(1);  // 参数 1 = ADC1_CH1
    
    // 根据设备型号选择按键映射
    if (MEMORY[0x3FCAA029] == 3) {
        // 设备 B 的映射逻辑
        if (adc_value > 3260 && adc_value <= 3999)  return 1;  // UP
        if (adc_value > 2408 && adc_value <= 3260)  return 2;  // DOWN
        if (adc_value > 1194 && adc_value <= 2408)  return 3;  // BACK (返回键)
        if (adc_value >= 0    && adc_value <= 1194)  return 4;  // OK
    } else {
        // 设备 A 的映射逻辑
        if (adc_value > 3260 && adc_value <= 3999)  return 1;  // OK
        if (adc_value > 2408 && adc_value <= 3260)  return 2;  // BACK (返回键)
        if (adc_value > 1194 && adc_value <= 2408)  return 3;  // DOWN
        if (adc_value >= 0    && adc_value <= 1194)  return 4;  // UP
    }
    
    return -1;  // 无按键按下或超出范围
}
```

---

## 4. 两款设备的按键映射对比

### 4.1 设备 A (型号标识符 ≠ 3)

| 按键 | ADC 原始值范围 | 功能 | 备注 |
|-----|----------------|------|------|
| 按键 1 | 3261 ~ 3999 | OK | 确认键 |
| **按键 2** | 2409 ~ 3260 | **BACK** | **返回键** |
| 按键 3 | 1195 ~ 2408 | DOWN | 向下 |
| 按键 4 | 0 ~ 1194 | UP | 向上 |

**按键布局**（从上到下）：
```
    [按键 4: UP]
    [按键 3: DOWN]
[按键 2: BACK] [按键 1: OK]
```

### 4.2 设备 B (型号标识符 == 3)

| 按键 | ADC 原始值范围 | 功能 | 备注 |
|-----|----------------|------|------|
| 按键 1 | 3261 ~ 3999 | UP | 向上 |
| 按键 2 | 2409 ~ 3260 | DOWN | 向下 |
| **按键 3** | 1195 ~ 2408 | **BACK** | **返回键** |
| 按键 4 | 0 ~ 1194 | OK | 确认键 |

**按键布局**（从上到下）：
```
[按键 1: UP] [按键 2: DOWN]
[按键 3: BACK] [按键 4: OK]
```

---

## 5. 返回键引脚定义

### 统一硬件连接

| 设备 | 返回键物理按键 | ADC 通道 | GPIO 引脚 | 寄存器地址 |
|-----|---------------|----------|-----------|-----------|
| **设备 A** | 按键 2 | ADC1_CH1 | **GPIO1** | 0x60003000 + 0x4 |
| **设备 B** | 按键 3 | ADC1_CH1 | **GPIO1** | 0x60003000 + 0x4 |

### ESP32-C3 GPIO1 特性

| 属性 | 值 |
|------|-----|
| GPIO 编号 | 1 |
| ADC1 通道 | 1 |
| ADC 范围 | 0 ~ 4095 (12-bit) |
| 输入电压范围 | 0 ~ 3.3V |
| 阻抗 | 约 100kΩ (建议) |

---

## 6. ADC 校准机制

### 6.1 校准值存储

校准值存储在 `eepromUser` NVS 命名空间中：

| 校准项 | NVS Key | 字符串地址 | 用途 |
|-------|----------|-----------|------|
| adcUP | "adcUP" | 0x3c1384b0 | 设备 A UP 键校准 |
| adcDOWN | "adcDOWN" | 0x3c1384b8 | 设备 A DOWN 键校准 |
| **adcBACK** | "adcBACK" | **0x3c1384c0** | 设备 A 返回键校准 |
| adcOK | "adcOK" | 0x3c1384c8 | 设备 A OK 键校准 |
| adcUP2 | "adcUP2" | 0x3c1384d0 | 设备 B UP 键校准 |
| adcDOWN2 | "adcDOWN2" | 0x3c1384d8 | 设备 B DOWN 键校准 |

### 6.2 校准函数

**函数地址**：`0x420152CA` (`sub_420152CA`)

**功能**：
1. 读取各按键的 ADC 原始值
2. 计算每个按键的典型 ADC 值
3. 保存到 NVS/eepromUser
4. 根据设备型号选择对应的校准值组

**调用者**：
- `sub_4202771E` (地址 `0x4202771E`) - 主按键处理函数
- `sub_42030520` (地址 `0x42030520`) - 设备初始化

---

## 7. 按键名称和本地化

### 7.1 按键名称（中文）

| 英文 | 中文 | 地址 |
|------|------|------|
| Left Key | 左键 | 0x3c13584c |
| Right Key | 右键 | 0x3c135858 |
| **Back Key** | **返回键** | **0x3c135864** |
| Ok Key | 确认键 | 0x3c135870 |

### 7.2 日文按键名称

| 英文 | 日文 | 说明 |
|------|------|------|
| Left Key | 左キー | 左键 |
| Right Key | 右キー | 右键 |
| Back Key | バックキー | 返回键 |
| Ok Key | OKキー | 确认键 |

---

## 8. 软件架构

### 8.1 按键扫描流程

```
                    ┌─────────────────┐
                    │  定时器中断     │
                    └────────┬────────┘
                             │
                             ▼
              ┌────────────────────────┐
              │  ADC1_CH1 读取       │
              │  (GPIO1)             │
              │  sub_420832FA(1)     │
              └────────┬───────────────┘
                       │
                       ▼
              ┌────────────────────────┐
              │  读取设备型号标识符    │
              │  (0x3FCAA029)        │
              └────────┬───────────────┘
                       │
            ┌──────────┴──────────┐
            │                     │
            ▼                     ▼
     ┌──────────┐        ┌──────────┐
     │ 设备 A   │        │ 设备 B   │
     │ 型号≠3   │        │ 型号==3  │
     └─────┬────┘        └────┬────┘
           │                  │
           ▼                  ▼
    按键2=BACK        按键3=BACK
    按键1=OK         按键4=OK
    按键3=DOWN        按键2=DOWN
    按键4=UP         按键1=UP
```

### 8.2 关键函数调用关系

```
entry (0x40381a38)
  └─> sub_4208F10A
       └─> sub_4202771E (主按键处理)
            ├─> sub_4200543C (ADC 读取和按键映射)
            └─> sub_420152CA (ADC 校准)
                 └─> 保存到 eepromUser
                      ├─ adcBACK (0x3c1384c0)
                      ├─ adcDOWN (0x3c1384b8)
                      ├─ adcUP (0x3c1384b0)
                      └─ adcOK (0x3c1384c8)
```

### 8.3 按键处理函数

**主按键处理**：`sub_4202771E` (地址 `0x4202771E`)

功能：
- 周期性扫描按键
- 处理长按和短按
- 根据按键编号触发相应功能
- 自动校准和去抖动

---

## 9. NVS 配置键

### 9.1 设备型号配置

```c
// 设备型号存储键
#define NVS_KEY_MODEL_TYPE    "modelType"
#define NVS_NAMESPACE_EEPUSER "eepUser"

// 设备型号定义
#define MODEL_TYPE_A           1   // 设备 A
#define MODEL_TYPE_B           3   // 设备 B
```

### 9.2 ADC 校准配置

```c
// 设备 A 的校准键
#define NVS_KEY_ADC_UP       "adcUP"
#define NVS_KEY_ADC_DOWN     "adcDOWN"
#define NVS_KEY_ADC_BACK     "adcBACK"
#define NVS_KEY_ADC_OK       "adcOK"

// 设备 B 的额外校准键
#define NVS_KEY_ADC_UP2      "adcUP2"
#define NVS_KEY_ADC_DOWN2    "adcDOWN2"
```

---

## 10. 总结

### 10.1 设计优势

1. **单一固件**：一个二进制文件支持多种硬件
2. **运行时适配**：自动检测设备类型并加载对应配置
3. **灵活可扩展**：可轻松添加新的设备类型
4. **用户可配置**：支持 ADC 校准以适应电阻公差
5. **成本优化**：减少固件维护和发布成本

### 10.2 技术要点

| 项目 | 说明 |
|------|------|
| MCU | ESP32-C3 |
| 按键检测 | ADC 电阻分压网络 |
| 返回键引脚 | GPIO1 (ADC1_CH1) - 两款设备相同 |
| 设备识别 | 0x3FCAA029 存储的型号标识符 |
| 校准存储 | NVS eepromUser 命名空间 |
| ADC 分辨率 | 12-bit (0-4095) |
| 按键数量 | 4 个物理按键 |

### 10.3 引脚汇总

| 功能 | GPIO | ADC 通道 | 备注 |
|------|------|----------|------|
| **返回键 (设备 A)** | **GPIO1** | **ADC1_CH1** | 按键编号 2 |
| **返回键 (设备 B)** | **GPIO1** | **ADC1_CH1** | 按键编号 3 |
| 其他按键 | GPIO1 | ADC1_CH1 | 通过不同电阻值区分 |

---

## 11. 附录：代码示例

### 11.1 设备型号检测

```c
// 获取设备型号
uint8_t device_model = MEMORY[0x3FCAA029];

// 判断设备类型
if (device_model == 3) {
    // 设备 B - 新版布局
    key_back_number = 3;
    key_ok_number = 4;
} else {
    // 设备 A - 旧版布局
    key_back_number = 2;
    key_ok_number = 1;
}
```

### 11.2 按键映射函数

```c
int get_key_number(int adc_value) {
    uint8_t model = MEMORY[0x3FCAA029];
    
    if (model == 3) {
        // 设备 B 映射
        if (adc_value > 3260) return 1;  // UP
        if (adc_value > 2408) return 2;  // DOWN
        if (adc_value > 1194) return 3;  // BACK
        return 4;                        // OK
    } else {
        // 设备 A 映射
        if (adc_value > 3260) return 1;  // OK
        if (adc_value > 2408) return 2;  // BACK
        if (adc_value > 1194) return 3;  // DOWN
        return 4;                        // UP
    }
}
```

---

**文档版本**：1.0  
**分析日期**：2026-01-26  
**固件文件**：app0.bin (MD5: 5837299fef2e633ba608440a337739a6)
